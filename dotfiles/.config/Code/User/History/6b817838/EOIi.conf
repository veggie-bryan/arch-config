###########################
# FLOAT + DASHBOARD GAPS
###########################

general {
    gaps_in = 8
    gaps_out = 10
    border_size = 2
    rounding = 8
}

decoration {
    active_opacity = 1.0
    inactive_opacity = 1.0
    blur = false
}

# Ensure gaps apply even to floating windows
dwindle {
    preserve_split = true
}

master {
    preserve_split = true
}

# Add padding so the top bar doesn’t overlap windows
groupbar {
    render_titles = false
}

# Extra padding below top bar (Waybar)
general {
    border_size = 2
    no_border_on_floating = false
}
